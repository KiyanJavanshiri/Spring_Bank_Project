spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bankdb
    username: root
    password: France_trump_JV-123
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: validate
      show-sql: false
      database-platform: org.hibernate.dialect.MySQL8Dialect
